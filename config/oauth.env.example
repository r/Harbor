# Harbor OAuth Provider Credentials
# =============================================================================
# These credentials enable Harbor's OAuth broker feature.
# When an MCP server has "clientIdSource: harbor", Harbor handles OAuth
# using these credentials instead of requiring users to create their own apps.
#
# Copy to: config/oauth.env
# Usage:   Baked into releases, can be overridden by users at runtime
# =============================================================================

# -----------------------------------------------------------------------------
# Google OAuth (for Gmail, Drive, Calendar, Sheets, etc.)
# -----------------------------------------------------------------------------
# Setup: https://console.cloud.google.com/apis/credentials
#
# 1. Create a project (e.g., "Harbor MCP Manager")
# 2. Configure OAuth consent screen:
#    - User type: External
#    - App name: Harbor
#    - Add scopes: gmail.modify, drive.readonly, calendar.events, etc.
# 3. Create OAuth client:
#    - Type: Desktop app
#    - Name: Harbor Desktop
# 4. Copy Client ID and Secret below
#
# Note: For production, submit app for Google verification.

HARBOR_GOOGLE_CLIENT_ID=""
HARBOR_GOOGLE_CLIENT_SECRET=""

# -----------------------------------------------------------------------------
# GitHub OAuth (for GitHub MCP servers)
# -----------------------------------------------------------------------------
# Setup: https://github.com/settings/developers
#
# 1. Go to Settings > Developer settings > OAuth Apps
# 2. Click "New OAuth App"
# 3. Fill in:
#    - Application name: Harbor
#    - Homepage URL: https://github.com/r/harbor
#    - Authorization callback URL: http://localhost:8765/oauth/callback
# 4. Copy Client ID and generate a Client Secret

HARBOR_GITHUB_CLIENT_ID=""
HARBOR_GITHUB_CLIENT_SECRET=""

# -----------------------------------------------------------------------------
# Microsoft OAuth (for Outlook, OneDrive, Teams, etc.)
# -----------------------------------------------------------------------------
# Setup: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps
#
# 1. Register a new application
# 2. Set redirect URI: http://localhost:8765/oauth/callback (Mobile/Desktop)
# 3. Add API permissions as needed
# 4. Create a client secret

HARBOR_MICROSOFT_CLIENT_ID=""
HARBOR_MICROSOFT_CLIENT_SECRET=""

# -----------------------------------------------------------------------------
# Slack OAuth (for Slack MCP servers)
# -----------------------------------------------------------------------------
# Setup: https://api.slack.com/apps
#
# 1. Create a new app
# 2. Add OAuth scopes as needed
# 3. Set redirect URL: http://localhost:8765/oauth/callback
# 4. Copy credentials from "OAuth & Permissions"

HARBOR_SLACK_CLIENT_ID=""
HARBOR_SLACK_CLIENT_SECRET=""

# -----------------------------------------------------------------------------
# OAuth Callback Port
# -----------------------------------------------------------------------------
# Port for the local OAuth callback server.
# Change if 8765 conflicts with something on your system.
# Note: Changing this port requires updating your OAuth app's callback URL.

HARBOR_OAUTH_CALLBACK_PORT="8765"

