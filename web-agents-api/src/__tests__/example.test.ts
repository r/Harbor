/**
 * Example unit test for web-agents-api.
 */

import { describe, it, expect } from 'vitest';

describe('Web Agents API Tests', () => {
  it('test infrastructure works', () => {
    expect(1 + 1).toBe(2);
  });
});
