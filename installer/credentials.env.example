# Harbor Credentials
# Copy this to credentials.env and fill in your values
# credentials.env is gitignored - never commit real credentials!
#
# This file contains two types of credentials:
#   1. BUILD CREDENTIALS - Used when building/signing/publishing Harbor
#   2. OAUTH CREDENTIALS - Baked into Harbor for user authentication flows

# =============================================================================
# SECTION 1: BUILD CREDENTIALS
# =============================================================================
# These are used by the build system and CI/CD pipelines.
# They are NOT distributed in the final application.

# -----------------------------------------------------------------------------
# Firefox Extension ID (REQUIRED for building)
# -----------------------------------------------------------------------------
# This must be unique per Mozilla Add-ons account. 
# Format: name@domain.com (letters, numbers, dots, hyphens, underscores only)
# 
# This ID is registered with Mozilla when you first sign the extension.

EXTENSION_ID="your.name.harbor@gmail.com"

# -----------------------------------------------------------------------------
# Mozilla Add-ons API (REQUIRED for extension signing)
# -----------------------------------------------------------------------------
# Get these from: https://addons.mozilla.org/developers/addon/api/key/
# 
# Steps:
# 1. Sign in to addons.mozilla.org
# 2. Go to Tools > Manage API Keys
# 3. Generate new credentials

AMO_JWT_ISSUER="user:12345678:123"
AMO_JWT_SECRET="your-64-character-hex-secret-here"

# -----------------------------------------------------------------------------
# Chrome Web Store (OPTIONAL)
# -----------------------------------------------------------------------------
# For Chrome/Chromium browser support. Leave empty until published.
#
# CHROME_EXTENSION_ID: The 32-character ID assigned by Chrome Web Store
#   - Format: 32 lowercase letters (a-p only), e.g., "abcdefghijklmnopabcdefghijklmnop"
#   - You get this after publishing to Chrome Web Store
#   - For dev mode, this is generated when you first load the unpacked extension
#
# CHROME_WEBSTORE_URL: Full URL to your extension's Web Store page
#   - Format: https://chrome.google.com/webstore/detail/harbor/EXTENSION_ID

CHROME_EXTENSION_ID=""
CHROME_WEBSTORE_URL=""

# Chrome Web Agents Extension (OPTIONAL)
# Separate extension for the Web Agents API
CHROME_WEB_AGENTS_EXTENSION_ID=""
CHROME_WEB_AGENTS_WEBSTORE_URL=""

# -----------------------------------------------------------------------------
# Apple Developer (OPTIONAL - for pkg signing and notarization)
# -----------------------------------------------------------------------------
# Required for distributing outside the App Store without Gatekeeper warnings.
# Requires Apple Developer Program membership ($99/year).
# If not set, the build will skip signing gracefully.

# Your Developer ID certificates
# Find yours with: security find-identity -v | grep "Developer ID"
#
# For signing binaries (harbor-bridge, apps) - required for notarization:
# DEVELOPER_ID_APPLICATION="Developer ID Application: Your Name (XXXXXXXXXX)"
#
# For signing .pkg installers:
# DEVELOPER_ID_INSTALLER="Developer ID Installer: Your Name (XXXXXXXXXX)"

# Your Apple ID email (for notarization)
# APPLE_ID="your@email.com"

# Your Apple Developer Team ID
# APPLE_TEAM_ID="XXXXXXXXXX"

# NOTE: You must also store an app-specific password in keychain:
#   xcrun notarytool store-credentials "AC_PASSWORD" \
#     --apple-id "your@email.com" \
#     --team-id "XXXXXXXXXX" \
#     --password "xxxx-xxxx-xxxx-xxxx"


# =============================================================================
# SECTION 2: OAUTH PROVIDER CREDENTIALS
# =============================================================================
# These credentials are BAKED INTO the distributed Harbor application.
# They enable "clientIdSource: harbor" OAuth flows where Harbor handles
# authentication on behalf of MCP servers.
#
# Each provider requires you to create an OAuth application in their
# developer console. The credentials should be for a "Desktop app" type.
#
# Users will see "Harbor wants to access..." when authenticating.

# -----------------------------------------------------------------------------
# Google OAuth (for Gmail, Drive, Calendar, Sheets MCP servers)
# -----------------------------------------------------------------------------
# Setup: https://console.cloud.google.com/apis/credentials
#
# 1. Create a project (or use existing)
# 2. Configure OAuth consent screen (External, published)
# 3. Add scopes: gmail.modify, drive.readonly, calendar.events, etc.
# 4. Create OAuth client ID (Desktop app type)
# 5. Copy Client ID and Client Secret below
#
# To test: Run the bridge with HARBOR_DEBUG_OAUTH=1

HARBOR_GOOGLE_CLIENT_ID=""
HARBOR_GOOGLE_CLIENT_SECRET=""

# -----------------------------------------------------------------------------
# GitHub OAuth (for GitHub MCP servers)
# -----------------------------------------------------------------------------
# Setup: https://github.com/settings/developers
#
# 1. Go to Settings > Developer settings > OAuth Apps
# 2. Click "New OAuth App"
# 3. Application name: Harbor
# 4. Homepage URL: https://github.com/anthropics/harbor (or your fork)
# 5. Authorization callback URL: http://localhost:8914/callback
# 6. Copy Client ID and Client Secret below

HARBOR_GITHUB_CLIENT_ID=""
HARBOR_GITHUB_CLIENT_SECRET=""

# -----------------------------------------------------------------------------
# Microsoft OAuth (for Outlook, OneDrive, Teams MCP servers)
# -----------------------------------------------------------------------------
# Setup: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps/ApplicationsListBlade
#
# 1. Register a new application
# 2. Supported account types: Personal + Work/School
# 3. Redirect URI: http://localhost:8914/callback (Mobile/Desktop)
# 4. Copy Application (client) ID
# 5. Create a client secret under Certificates & secrets

HARBOR_MICROSOFT_CLIENT_ID=""
HARBOR_MICROSOFT_CLIENT_SECRET=""

# -----------------------------------------------------------------------------
# Slack OAuth (for Slack MCP servers)
# -----------------------------------------------------------------------------
# Setup: https://api.slack.com/apps
#
# 1. Create New App > From scratch
# 2. Add OAuth scopes under OAuth & Permissions
# 3. Add redirect URL: http://localhost:8914/callback
# 4. Copy Client ID and Client Secret

HARBOR_SLACK_CLIENT_ID=""
HARBOR_SLACK_CLIENT_SECRET=""


# =============================================================================
# SECTION 3: OPTIONAL RUNTIME CONFIGURATION  
# =============================================================================
# These override default Harbor behavior. Usually not needed.

# Default LLM provider for chat (ollama, llamafile, or openai-compatible URL)
# HARBOR_DEFAULT_LLM_PROVIDER=""

# Enable debug logging in production builds
# HARBOR_DEBUG="false"
